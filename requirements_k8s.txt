# The app will not work in k8s unless all these packages are installed

# Core Flask dependencies
Flask==3.1.3
Flask_Session==0.8.0
gunicorn==22.0.0

# Database
SQLAlchemy==2.0.31
Flask_SQLAlchemy==3.1.1
psycopg2-binary==2.9.12

# needed by app for image post processing
numpy==2.0.2
opencv-python==4.12.0.88
openvino==2025.3.0
supervision==0.25.1

Flask_WTF==1.2.1
pyzmq==26.0.3

# Colonizer connects to Valkey via Redis protocol
redis==5.1.0 # this is the python redis client - not the server. Flask session requires it